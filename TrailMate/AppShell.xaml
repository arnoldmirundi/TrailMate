<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="TrailMate.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:views="clr-namespace:TrailMate.Views"
    Shell.FlyoutBehavior="Disabled">

    <TabBar>
        <ShellContent Title="Home"     Route="HomePage"
                      ContentTemplate="{DataTemplate views:HomePage}"/>
        <ShellContent Title="Tracker"  Route="TrailTrackerPage"
                      ContentTemplate="{DataTemplate views:TrailTrackerPage}"/>
        <ShellContent Title="Camera"   Route="CameraPage"
                      ContentTemplate="{DataTemplate views:CameraPage}"/>
        <ShellContent Title="Compass"  Route="CompassPage"
                      ContentTemplate="{DataTemplate views:CompassPage}"/>
        <ShellContent Title="Settings" Route="SettingsPage"
                      ContentTemplate="{DataTemplate views:SettingsPage}"/>
    </TabBar>

</Shell>